<Window x:Class="OrderServiceClient.Views.ConfirmOrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ConfirmOrderView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="OrderDetails" Grid.ColumnSpan="2" />
        <Button x:Name="Accept" Content="Accept" Grid.Row="1" Grid.Column="0" />
        <Button x:Name="Reject" Content="Reject" Grid.Row="1" Grid.Column="1" />
    </Grid>
</Window>
