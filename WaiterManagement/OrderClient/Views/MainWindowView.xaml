<Window x:Class="OrderClient.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="400" Width="525" ResizeMode="NoResize">
    <Grid Style="{DynamicResource ResourceKey=BackgroundGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <ContentControl x:Name="ActiveItem" />
        <Button x:Name="Logout" Content="Log Out" Margin="10" Grid.Row="1" />
        <Button x:Name="AddNewOrder" Content="New Order" Margin="10" Grid.Row="2" />
    </Grid>
</Window>
